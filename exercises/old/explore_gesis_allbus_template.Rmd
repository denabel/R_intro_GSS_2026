---
title: "Exploring data from the GESIS German General Social Survey - ALLBUS 2021"
author: "Dennis"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_depth: 3
    number_sections: true
    toc_float: true
    code_folding: hide
    theme: flatly
    highlight: tango
    code_download: true
    df_print: paged
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
                      warning = FALSE,
                      message = FALSE)

options(scipen = 15)
```

## Data Wrangling

```{r load-wrangle-data, echo = FALSE}
library(easypackages)

packages("rmarkdown", "knitr", "tidyverse", "visdat", "janitor", "pander", "patchwork", "correlation", "GGally", "broom", "sjPlot", "scales", "sjlabelled", prompt = F)

inverter2 <- function (var) {max(var, na.rm = TRUE) - var + 1}

allbus_2021 <- 
  haven::read_sav(
    "../data/allbus_2021/ZA5280_v1-0-0.sav"
  ) %>% 
  sjlabelled::set_na(na = c(-1:-99, 97, 98)) %>% 
  rowwise() %>%
  mutate(
    sum_trust = 
      sum(
        c_across(pt01:pt20),
        na.rm = TRUE
      ),
    distrust = inverter2(sum_trust)
  ) %>% 
 
  remove_all_labels() %>% 
  ungroup() %>% 
  as_tibble()

plot(allbus_2021$sum_trust, allbus_2021$distrust)
```

# The data

The data we use comes from the [German General Social Survey - ALLBUS 2021 ](https://search.gesis.org/research_data/ZA5280).We have selected ... variables of interest for our analysis. The dataset includes data for *N* = ... respondents. 


## Missing data

As not all of the respondents answered every question in the survey, we have missing data. Before we further explore the data, we want to know whether there are identifiable patterns of missingness in them.

```{r vis-miss}

```

# Exploring the *German General Social Survey - ALLBUS 2021* dataset

## Demographics

We first want to look at gender, age (categories), education, and party vote in our sample.

```{r demo-tables}

```

## Summary statistics

Let's look at the summary statistic for the items on xenophobia.

```{r summarystats}

```

## Distribution of variables

We want to explore how some of the data for the question on life satisfaction and political orientation (left-right) are distributed in our sample.

```{r density-satf}

```

```{r density-lr}

```

## Party affiliation and xenophobic attitudes

How do xenophobic attitudes differ between supporters of different parties? 

```{r party-xenoph}

```

# Analyses

## Correlations

We want to explore the correlations between political leaning, xenophobia, and life satisfaction.

```{r correlations}
  
```

## Regression analysis

We use a logistic regression model to explore how trust in people is predicted by life satisfaction, mean xenophobic attitudes and the contact with foreigners.

```{r regression}

```

The unstandardized regression coefficients *b* can be selected from the `tidy_model`.

### Regression plots

```{r regression-plots}

```
